<html>

<head>
    <title>User</title>
</head>

<body>
    <ul>
        <% messages.forEach((message) =>{ %>
            <%- include('message',{message: message.text}); %>
            <% }); %>
    </ul>
</body>

</html>